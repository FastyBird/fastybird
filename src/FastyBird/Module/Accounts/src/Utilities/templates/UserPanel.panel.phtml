<?php
declare(strict_types=1);

namespace FastyBird\Module\Accounts\Utilities;

use Tracy\Dumper;

?>
<div class="nette-UserPanel">
	<h1><?php if ($user->isLoggedIn()): ?>Logged in<?php else: ?>Unlogged<?php endif ?></h1>

	<?php if ($user->getIdentity()): echo Dumper::toHtml($user->getIdentity()->toArray(), [Dumper::LIVE => true]); else: ?><p>no identity</p><?php endif ?>
</div>
